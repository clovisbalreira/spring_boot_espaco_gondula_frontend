/* eslint-disable jsx-a11y/anchor-is-valid */
import React from "react";
import { Row, Col, Card, Input, Button} from 'react-materialize';
import ProviderService from '../../../../services/provider-service';

export default function Delete(){
  return ( 
    <Row>
   </Row>        
  );
}
/**
 *       <Card class="card-panel">
        <span class="blue-text text-darken-2">Deletar Mercado</span>
      </Card>

      <Col s={9}>
        <Row class="input-field">
          <Input 
            placeholder="Digite o código" 
            label="Código" 
            s={12}
            //value={name}
            //onChange={handleNameChange}
          />
        </Row>
      </Col>

      <Col s={3}>
        <Button class="waves-effect waves-light btn">Deletar</Button>
      </Col>
    
      <Col s={12}>
        <Row class="input-field">
          <Input 
            placeholder="Digite o mercado" 
            label="Mercado" 
            s={12}
            //value={name}
            //onChange={handleNameChange}
          />
        </Row>
      </Col>

      <Col s={6}>
        <Row class="input-field">
          <Input 
            placeholder="Digite o gondola" 
            label="Gondola" 
            s={12}
            //value={name}
            //onChange={handleNameChange}
          />
        </Row>
      </Col>

      <Col s={6}>
        <Row class="input-field row">
          <Input 
            placeholder="Digite o plateleira" 
            label="Plateleira" 
            s={12}
            //value={name}
            //onChange={handleNameChange}
          />
        </Row>
      </Col>
 
 */