/* eslint-disable jsx-a11y/anchor-is-valid */
import React from "react";
import { Row, Col, Card, Input, Button} from 'react-materialize';
import ProviderService from '../../../../services/provider-service';

export default function Save(){
  return ( 
      <Row>
     </Row>        
  );
}
/**
 *         <Card class="card-panel">
          <span class="blue-text text-darken-2">Adicionar Mercado</span>
        </Card>

        <Col s={12}>
          <Row class="input-field">
            <Input 
              placeholder="Digite o mercado" 
              label="Mercado" 
              s={12}
              //value={name}
              //onChange={handleNameChange}
            />
          </Row>
        </Col>

        <Col s={4}>
          <Row class="input-field">
            <Input 
              placeholder="Digite o gondola" 
              label="Gondola" 
              s={12}
              //value={name}
              //onChange={handleNameChange}
            />
          </Row>
        </Col>

        <Col s={4}>
          <Row class="input-field">
            <Input 
              placeholder="Digite o plateleira" 
              label="Plateleira" 
              s={12}
              //value={name}
              //onChange={handleNameChange}
            />
          </Row>
        </Col>

        <Col s={4}>
            <Button class="waves-effect waves-light btn">Salvar</Button>
        </Col>
 
 */